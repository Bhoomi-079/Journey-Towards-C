#include <stdio.h>

int main() {
    int score = 10;

    printf("Initial score: %d\n", score);

    score++; 
    printf("After incrementing: %d\n", score);

    score--; 
    printf("After decrementing: %d\n", score);

    return 0;
}
